{
  "name": "sample-nodejs-app",
  "version": "1.0.0",
  "description": "A sample Node.js app for testing npm ci, build, and test",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "build": "node build.js",
    "test": "jest",
    "ci": "npm ci && npm test && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
