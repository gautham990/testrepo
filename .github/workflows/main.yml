name: Caching with npm
on: 
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: env
        run: |  
          echo $GITHUB_REF
          echo $GITHUB_REF_NAME

