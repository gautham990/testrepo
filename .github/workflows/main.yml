name: test-job
on:
  push:

jobs:
  big-fix:
    runs-on: ubuntu-latest
    if: ${{ contains(github.ref, 'bugfix/**')}}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Get the branch name
        run:
          echo $GITHUB_REF

          

      
