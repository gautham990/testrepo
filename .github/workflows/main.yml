name: CI
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: lob/action_sam_jackson@0.0.2
      with:
          message: 'Hello'
          token: ${{ secrets.PAT }}
