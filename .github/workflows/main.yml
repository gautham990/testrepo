name: CI
on: push
env:
  FIRST_NAME: Salamanca
jobs:
  prod-check:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Nice work, $FIRST_NAME"
        env:
          FIRST_NAME: Tuco
